import React = require('react');

export const Contact = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'right',
        alignItems: 'right',
        height: '100vh',
      }}
    >
      Contact pages
    </div>
  );
};
