import React = require('react');

export const Products = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'right',
        alignItems: 'right',
        height: '100vh',
      }}
    >
      Products pages
    </div>
  );
};
