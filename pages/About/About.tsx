import React = require('react');

export const About = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'right',
        alignItems: 'right',
        height: '100vh',
      }}
    >
      About pages
    </div>
  );
};
